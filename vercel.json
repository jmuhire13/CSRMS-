{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd csrms-frontend && npm install && npm run build",
  "outputDirectory": "csrms-frontend/dist",
  "installCommand": "cd csrms-frontend && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
